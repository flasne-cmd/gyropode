[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
; Bluetooth classique + Wi-Fi + serveur web dépassent la partition par défaut
board_build.partitions = huge_app.csv
build_flags =
    -Wall
    -DCORE_DEBUG_LEVEL=1
